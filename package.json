{
  "name": "vaultic-trust",
  "version": "1.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "install:refresh": "yarn cache clean && yarn install",
    "format": "yarn next:format",
    "lint": "yarn next:lint",
    "next:build": "yarn workspace @vaultic/nextjs build",
    "next:check-types": "yarn workspace @vaultic/nextjs check-types",
    "next:format": "yarn workspace @vaultic/nextjs format",
    "next:lint": "yarn workspace @vaultic/nextjs lint",
    "next:serve": "yarn workspace @vaultic/nextjs serve",
    "precommit": "lint-staged",
    "start": "yarn workspace @vaultic/nextjs dev",
    "vercel": "yarn workspace @vaultic/nextjs vercel",
    "vercel:login": "yarn workspace @vaultic/nextjs vercel:login"
  },
  "devDependencies": {
    "husky": "~9.1.6",
    "lint-staged": "~13.2.2"
  },
  "packageManager": "yarn@3.2.3",
  "engines": {
    "node": ">=20.18.3"
  }
}
