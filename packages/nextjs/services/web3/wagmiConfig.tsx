/**
 * DEPRECATED — EVM wagmi config replaced by Stellar Horizon client.
 * Kept as a stub so legacy imports don't cause TS errors during migration.
 * Remove this file entirely in Phase 2 cleanup.
 * @see services/stellar/horizonClient.ts
 */
export const wagmiConfig = null;
export const queryClient = null;
export const enabledChains: never[] = [];
