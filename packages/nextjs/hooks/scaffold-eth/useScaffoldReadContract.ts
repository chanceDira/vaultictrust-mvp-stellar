/**
 * DEPRECATED — EVM read contract hook. Stub for Stellar migration.
 */
export function useScaffoldReadContract(_config: any) {
  return { data: undefined, isLoading: false, refetch: () => Promise.resolve() };
}
